import Point2DImplicits._

//object Main extends App {
/* object Main {
  val list = new MyList[Point2D]
  list.add(Point2D(1, 1));
  list.add(Point2D(2, 2));
  list.addAt(0, Point2D(3, 3));
  list.addAt(3, Point2D(4, 4));
  list.delete(Point2D(1, 1));
  list.add(Point2D(6, 6));
  list.deleteAt(0);
  list.deleteAt(2);
  list.addAt(0, Point2D(7, 7));
  list.addAt(2, Point2D(1, 1));
  // 77 22 11 44

  println("Non sorted list: ")
  list.traverse(println)
  println("---------")
  val sortedList = MyList.mergeSortMyListImp(list)
  println("Sorted list: ")
  sortedList.traverse(println)
  println("---------")
  
  println("List elements:")
  list.traverse(println) */

  /* val array: Array[Int] = Array(4, 3, 2, 5, 1)
  array.foreach(println)
  println("-----")
  val sortedArr = MyList.mergeSortImperative(array)
  sortedArr.foreach(println)
} */